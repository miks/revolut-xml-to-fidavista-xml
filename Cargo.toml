[package]
name = "revolut2fidavista"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1.3"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }

[profile.release]
opt-level = "z"
strip = true
lto = true
